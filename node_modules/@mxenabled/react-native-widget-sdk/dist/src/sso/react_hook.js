import { useEffect, useState } from "react";
import { getSsoUrl } from "./request";
export function useSsoUrl(props) {
    const [url, setUrl] = useState(undefined);
    useEffect(() => {
        getSsoUrl(props).then(setUrl);
    }, []);
    return url;
}
